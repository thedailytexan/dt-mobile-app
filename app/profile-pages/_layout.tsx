import { Stack } from "expo-router";

export default function ProfilePagesLayout() {
  return (
    <Stack
      screenOptions={{
        headerTitle: "",  
        headerShadowVisible: false, 
        headerBackTitle: "",
      }}
    />);
}